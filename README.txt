this is a site-specific place, should only cloned once at the beginning and should never sync again from 
github unless to "reset to factory default".

add this to profile
   . <full site-spec dir>/profile
